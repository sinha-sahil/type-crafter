/**
 * @type { {{enumName}} }
 {{#if description}}
 * @description {{{description}}}
 {{/if}}
 {{#if example}}
 * @example {{{example}}}
 {{/if}}
 */
export type {{enumName}} =
{{#each values}}
  | {{#if (eq ../enumType 'string') }}'{{/if}}{{{this}}}{{#if (eq ../enumType 'string')}}'{{/if}}
{{/each}};
